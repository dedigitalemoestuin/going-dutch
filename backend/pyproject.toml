[project]
name = "going-dutch"
version = "0.1.0"
description = "Bill splitter"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "django==6.1",
]

[tool.uv]
package = false
exclude-newer = "1 week"
